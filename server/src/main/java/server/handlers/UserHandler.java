package server.handlers;

import exception.ResponseException;
import server.service.Service;

//endpoints: register, login/logout
public class UserHandler {

    public UserHandler(){}

    /*public String[] login(String username, String password, Service service) throws ResponseException {
        return service.login(username, password);
    }

    public void register(String username, String password, String email, Service service) throws ResponseException {
        service.register(username, password, email);
    }*/
}
